import React from "react";
import IPage from "interfaces/page.interface";
import * as s from "./Main.styles";

const Main: React.FunctionComponent<IPage> = (props) => {
  return <s.Main>MAIN PAGE DUDE</s.Main>;
};

export default Main;
